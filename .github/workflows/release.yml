name: Deploy

on:
  push:
    tags:
      - '*.*.*'
      - '*.*.*-*'

permissions:
  contents: write

jobs:
  build-and-upload:
    name: Build and upload
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - build: linux-musl
            os: ubuntu-latest
            target: x86_64-unknown-linux-musl

          - build: linux-aarch64
            os: ubuntu-latest
            target: aarch64-unknown-linux-gnu

          - build: linux-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu

          - build: macos-x86_64
            os: macos-latest
            target: x86_64-apple-darwin

          - build: macos-aarch64
            os: macos-latest
            target: aarch64-apple-darwin

          - build: windows
            os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Get the release version from the tag
        shell: bash
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f  # v6.3.0
        with:
          node-version: '22'

      - name: Update extension version
        shell: bash
        run: |
          # Update dsdk-vscode package.json version to match release tag
          sed -i.bak 's/"version": "[^"]*"/"version": "${{ env.VERSION }}"/' dsdk-vscode/package.json
          rm dsdk-vscode/package.json.bak
          echo "Updated extension version to ${{ env.VERSION }}"
          grep '"version"' dsdk-vscode/package.json

      - name: Build VS Code Extension
        shell: bash
        run: |
          cd dsdk-vscode
          npm ci
          npm run vscode:prepublish
          npx vsce package
          echo "VSIX_FILE=$(ls -1 *.vsix | head -1)" >> $GITHUB_ENV
          cd ..
          echo "VSIX_FILE=${{ env.VSIX_FILE }}"

      - name: Restore clean git state
        shell: bash
        run: |
          # The "Update extension version" step modified dsdk-vscode/package.json
          # which is a tracked file. Without restoring, build.rs detects uncommitted
          # changes via `git diff-index` and marks the binary as "-dirty".
          git checkout -- .

      - name: Install Rust
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9  # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.os == 'ubuntu-latest'
        run: cargo install cross --locked

      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            cross build --verbose --release --target ${{ matrix.target }}
          else
            cargo build --verbose --release --target ${{ matrix.target }}
          fi

      - name: Build archive
        shell: bash
        run: |
          package_name="cim-suite"
          cli_binary="cim"

          dirname="$package_name-${{ env.VERSION }}-${{ matrix.target }}"
          mkdir "$dirname"

          # Move binary and verify it exists
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cli_path="target/${{ matrix.target }}/release/$cli_binary.exe"
          else
            cli_path="target/${{ matrix.target }}/release/$cli_binary"
          fi

          if [ ! -f "$cli_path" ]; then
            echo "Error: CLI binary not found at $cli_path"
            exit 1
          fi

          mv "$cli_path" "$dirname/"

          # Copy VS Code extension (platform-agnostic)
          if [ -f "dsdk-vscode/${{ env.VSIX_FILE }}" ]; then
            cp "dsdk-vscode/${{ env.VSIX_FILE }}" "$dirname/"
            echo "Included VS Code extension: ${{ env.VSIX_FILE }}"
          else
            echo "Warning: VS Code extension not found at dsdk-vscode/${{ env.VSIX_FILE }}"
          fi

          # Create a simple README for the archive
          cat > "$dirname/README.txt" << 'EOF'
          Code in Motion
          ==============

          This archive contains the cim toolkit:

          Binaries:
          - cim: Command-line interface for workspace management

          Extensions:
          - cim-*.vsix: VS Code extension for cim task integration
            Install in VS Code via: Extensions → Install from VSIX...

          Usage:
          ------
          For CLI usage: ./cim --help
          For VS Code: Install the .vsix file via the Extensions menu

          For more information, visit:
          https://github.com/analogdevicesinc/cim
          EOF

          # Verify binary was moved successfully
          echo "Archive contents:"
          ls -la "$dirname"

          # Create archive
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            7z a "$dirname.zip" "$dirname"
            echo "ASSET=$dirname.zip" >> $GITHUB_ENV
          else
            tar -czf "$dirname.tar.gz" "$dirname"
            echo "ASSET=$dirname.tar.gz" >> $GITHUB_ENV
          fi

      - name: Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe  # v2.6.1
        with:
          files: |
            ${{ env.ASSET }}
